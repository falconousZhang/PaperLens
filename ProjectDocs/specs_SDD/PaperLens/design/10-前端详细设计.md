# 前端详细设计

## 文档信息

| 项目 | 内容 |
|------|------|
| 项目名称 | PaperLens |
| 文档版本 | v1.0 |
| 创建日期 | 2026-07-13 |
| 最后更新 | 2026-07-13 |

## 1. 技术栈

| 技术 | 版本 | 说明 |
|------|------|------|
| Vue3 | 3.x | 组合式 API + `<script setup>` |
| TypeScript | - | 类型安全 |
| Element Plus | - | PC 端 UI 组件库 |
| Pinia | 2.x | 状态管理 |
| Axios | - | HTTP 请求封装 |
| Vue Router | 4.x | 路由管理 |
| Vite | - | 构建工具 |

## 2. 项目结构

```
frontend/
├── src/
│   ├── api/              # API 请求封装
│   │   ├── papers.ts     # 论文相关 API
│   │   ├── tasks.ts      # 任务相关 API
│   │   ├── evidences.ts  # 证据相关 API
│   │   ├── reviews.ts    # 审阅相关 API
│   │   ├── metrics.ts    # 指标相关 API
│   │   ├── experiments.ts # 实验数据相关 API
│   │   └── exports.ts    # 导出相关 API
│   ├── components/       # 通用组件
│   │   ├── FileUpload.vue    # 文件上传组件
│   │   ├── StatusTag.vue     # 状态标签组件
│   │   ├── EvidenceHighlight.vue # Evidence 高亮组件
│   │   └── PollingWrapper.vue # 轮询包装组件
│   ├── views/            # 页面视图
│   │   ├── HomeView.vue         # 首页 (P01)
│   │   ├── UploadView.vue       # 论文上传 (P02)
│   │   ├── PaperListView.vue    # 论文列表 (P03)
│   │   ├── PaperDetailView.vue  # 论文详情 (P04)
│   │   ├── ReviewResultView.vue # 审阅结果 (P05, 规划)
│   │   ├── MetricAnalysisView.vue # 指标分析 (P06, 规划)
│   │   ├── ExperimentDataView.vue # 实验数据 (P07, 规划)
│   │   └── ReportExportView.vue # 报告导出 (P08, 规划)
│   ├── stores/           # Pinia 状态管理
│   │   ├── paper.ts      # 论文状态
│   │   └── task.ts       # 任务状态
│   ├── router/           # 路由配置
│   │   └── index.ts
│   └── utils/            # 工具函数
│       ├── request.ts    # Axios 封装
│       └── highlight.ts  # Evidence 高亮工具
```

## 3. 路由设计

### 3.1 路由配置

```typescript
const routes = [
  { path: '/', name: 'home', component: HomeView },
  { path: '/upload', name: 'upload', component: UploadView },
  { path: '/papers', name: 'papers', component: PaperListView },
  {
    path: '/papers/:id',
    component: PaperDetailLayout,
    children: [
      { path: '', name: 'paper-detail', component: PaperDetailContent },
      { path: 'review', name: 'paper-review', component: ReviewResultView },
      { path: 'metrics', name: 'paper-metrics', component: MetricAnalysisView },
      { path: 'experiment', name: 'paper-experiment', component: ExperimentDataView },
      { path: 'export', name: 'paper-export', component: ReportExportView },
    ]
  }
]
```

### 3.2 导航架构

```
首页 (/)
  ├── 上传论文 (/upload)
  └── 论文列表 (/papers)
        └── 论文详情 (/papers/:id)
              ├── 章节 Tab
              ├── 页面 Tab
              ├── 证据 Tab
              ├── 审阅 (/papers/:id/review)
              ├── 指标 (/papers/:id/metrics)
              ├── 实验 (/papers/:id/experiment)
              └── 导出 (/papers/:id/export)
```

## 4. 核心页面设计

### 4.1 P01 首页 (HomeView)

**组件结构:**

- 标题区: 项目名 + 副标题
- 状态卡: 后端健康检查结果
- 操作按钮: 上传论文 (primary) + 论文列表 (secondary)

**数据流:**

- 页面加载 → `GET /api/v1/health` → 更新状态卡
- 点击"上传论文" → 路由跳转 `/upload`
- 点击"论文列表" → 路由跳转 `/papers`

### 4.2 P02 论文上传 (UploadView)

**组件结构:**

- 拖拽区: 虚线边框，支持 drag-drop 和点击选择
- 文件信息: 文件名和大小
- 进度条: 上传进度百分比
- 错误提示: 非 PDF / 超过 50MB
- 上传按钮

**数据流:**

- 选择/拖拽文件 → 校验类型和大小
- 点击"上传" → `POST /api/v1/papers/upload`
- 上传成功 → 路由跳转 `/papers/:id`

### 4.3 P03 论文列表 (PaperListView)

**组件结构:**

- 表格: 标题、文件名、状态、页数、创建时间、操作
- 状态标签: 解析中(橙) / 已解析(绿) / 失败(红) / 上传中(灰)
- 分页: 页码导航

**数据流:**

- 页面加载 → `GET /api/v1/papers`
- PROCESSING 状态 → 3 秒间隔轮询
- 点击"查看" → 路由跳转 `/papers/:id`

### 4.4 P04 论文详情 (PaperDetailView)

**组件结构:**

- 元信息栏: 文件名、大小、页数、状态标签
- Tab 切换: 章节 / 页面 / 证据
- 章节卡片列表
- 页面内容 + Evidence 高亮
- 证据列表

**Evidence 高亮核心逻辑:**

```typescript
function highlightEvidence(
  normalizedText: string,
  charStart: number | null,
  charEnd: number | null,
  quotedText: string
): { highlighted: string; degraded: boolean } {
  if (charStart === null || charEnd === null) {
    return { highlighted: normalizedText, degraded: true }
  }
  const extracted = normalizedText.substring(charStart, charEnd)
  if (extracted !== quotedText || charStart < 0 || charEnd > normalizedText.length) {
    return { highlighted: normalizedText, degraded: true }
  }
  const before = normalizedText.substring(0, charStart)
  const match = normalizedText.substring(charStart, charEnd)
  const after = normalizedText.substring(charEnd)
  return {
    highlighted: `${before}<mark class="evidence-highlight">${match}</mark>${after}`,
    degraded: false
  }
}
```

**数据流:**

- 页面加载 → `GET /api/v1/papers/:id` + `GET /api/v1/papers/:id/sections` + `GET /api/v1/papers/:id/evidences`
- PROCESSING 状态 → 3 秒轮询
- 切换页面 Tab → `GET /api/v1/papers/:id/pages/:page_number`
- 点击证据 → 切换到页面 Tab + 跳转页码 + 高亮

### 4.5 P05 审阅结果 (ReviewResultView) — 规划

**组件结构:**

- 审阅概览: 总评分、维度数、Finding 总数
- 维度卡片: dimension 分组，评分和摘要
- Finding 卡片: 类型标签、内容、置信度、证据链接
- 筛选: STRENGTH / WEAKNESS / SUGGESTION

**数据流:**

- 页面加载 → `GET /api/v1/papers/:id/reviews`
- 点击"查看证据" → 切换到页面 Tab + 高亮
- 无审阅结果 → 显示"发起审阅"按钮 → `POST /api/v1/papers/:id/tasks`
- 审阅进行中 → 3 秒轮询

### 4.6 P06 指标分析 (MetricAnalysisView) — 规划

**组件结构:**

- 筛选栏: 模型名、数据集、口径下拉
- 指标表格: 模型名、数据集、指标名、值、口径类型、口径来源
- 行展开: 来源证据、表格行号、证据链接

**数据流:**

- 页面加载 → `GET /api/v1/papers/:id/metrics`
- 筛选变更 → 重新请求或前端过滤
- 点击"查看证据" → 切换到页面 Tab + 高亮

### 4.7 P07 实验数据 (ExperimentDataView) — 规划

**组件结构:**

- 上传区: 拖拽/选择 CSV/XLSX/XLS
- 文件列表: 文件名、类型、行列数、删除
- 统计摘要表格: count/mean/std/min/max/median
- 交叉验证: MATCH(绿)/MISMATCH(红)

**数据流:**

- 上传文件 → `POST /api/v1/papers/:id/experiment-files/upload`
- 页面加载 → `GET /api/v1/papers/:id/experiment-files`
- 选择文件 → `GET /api/v1/experiment-files/:file_id/result`

### 4.8 P08 报告导出 (ReportExportView) — 规划

**组件结构:**

- 导出配置: 格式选择、语言选择、包含选项
- 生成按钮
- 导出历史: 状态、文件大小、下载操作

**数据流:**

- 点击"生成报告" → `POST /api/v1/papers/:id/exports`
- 生成中 → 3 秒轮询 `GET /api/v1/exports/:export_id`
- 点击"下载" → `GET /api/v1/exports/:export_id/download`

## 5. 核心组件设计

### 5.1 FileUpload.vue

**Props:**

| 属性 | 类型 | 说明 |
|------|------|------|
| accept | String | 接受的文件类型 |
| maxSize | Number | 最大文件大小（字节） |
| uploading | Boolean | 是否上传中 |

**Events:**

| 事件 | 说明 |
|------|------|
| upload | 文件上传请求 |
| error | 校验错误 |

### 5.2 EvidenceHighlight.vue

**Props:**

| 属性 | 类型 | 说明 |
|------|------|------|
| normalizedText | String | 归一化页面文本 |
| charStart | Number/null | 起始字符偏移 |
| charEnd | Number/null | 结束字符偏移 |
| quotedText | String | 证据引用文本 |

**功能:**

- 基于 char_start / char_end 高亮文本
- 降级提示: char 区间无效时显示橙色提示条
- 自动 scrollIntoView

### 5.3 PollingWrapper.vue

**Props:**

| 属性 | 类型 | 说明 |
|------|------|------|
| interval | Number | 轮询间隔（毫秒），默认 3000 |
| polling | Boolean | 是否轮询中 |

**功能:**

- 定时调用回调函数
- 组件卸载时清除定时器
- 防止多个定时器并存

## 6. 状态管理设计

### 6.1 paper store

```typescript
interface PaperState {
  papers: Paper[]
  currentPaper: Paper | null
  sections: Section[]
  currentPage: Page | null
  evidences: Evidence[]
  loading: boolean
  error: string | null
}
```

### 6.2 task store

```typescript
interface TaskState {
  tasks: Task[]
  currentTask: Task | null
  polling: boolean
}
```

## 7. 设计规范

### 7.1 布局规范

| 项目 | 规范 |
|------|------|
| 页面最大宽度 | 960px（居中） |
| 页面内边距 | 2rem 上下，1rem 左右 |
| 主色调 | #1a1a2e（深蓝黑） |
| 卡片圆角 | 8px |
| 按钮圆角 | 6-8px |

### 7.2 状态色规范

| 状态 | 文字色 | 背景色 | 用途 |
|------|--------|--------|------|
| 正常/成功 | #2e7d32 | #e8f5e9 | 已解析、MATCH、健康 |
| 警告/进行中 | #f57c00 | #fff3e0 | 解析中、PROCESSING |
| 错误/失败 | #c62828 | #ffebee | 失败、MISMATCH、错误 |
| 高亮 | - | #fff176 | Evidence 高亮 |
| 降级 | #e65100 | #fff3e0 | 高亮降级提示 |

### 7.3 交互规范

| 场景 | 规范 |
|------|------|
| 加载状态 | 显示"加载中..."文字提示 |
| 轮询间隔 | 3 秒 |
| 错误提示 | 红色背景卡片，含重试按钮 |
| 请求去重 | 使用 request id 防止陈旧响应覆盖 |

## 8. 测试覆盖

| 页面 | 测试数 | 状态 |
|------|--------|------|
| P04 论文详情 | 15 | 已覆盖 |
| P01-P03 | - | 待补充 |
| P05-P08 | - | 规划 |

---

**文档版本**: v1.0
**创建日期**: 2026-07-13
**最后更新**: 2026-07-13