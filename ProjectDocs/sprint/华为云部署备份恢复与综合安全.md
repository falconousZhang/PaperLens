# P8.4 华为云部署、备份恢复与综合安全 Sprint

## 文档信息

- 阶段：P8.4（最后一个既定开发轮次）
- 状态：已完成并经码道独立收口
- 实现、审查、修正与验收：CodeArts（码道）
- 日期：2026-07-16

## 完成项

- [x] OBSStorage：ECS Agency/ENV、私有 ACL、SSE-OBS/KMS、2xx 检查、严格 key、脱敏错误。
- [x] `materialize`：Local/OBS 统一上下文生命周期，三个生产调用方完成迁移。
- [x] 配置 fail-closed：华为端点、强 JWT、RDS verify-full/CA、MaaS/Embedding Key、受限代理网段、生产文档关闭。
- [x] Secret 文件入口：serve/migrate 共用数据库、JWT、MaaS 和可选 OBS Secret 加载。
- [x] 后端生产镜像：Python 3.13、非 root、独立生产依赖、不包含测试和环境文件。
- [x] 前端生产镜像：修正构建上下文，非 root Nginx 8080，pid/临时目录使用 tmpfs。
- [x] Compose：独立 migrate、固定私网、仅发布前端、ready 健康检查、只读/最小权限/资源限制。
- [x] 部署、OBS ENV 覆盖、备份恢复、安全验收和回滚文档。
- [x] systemDesign 01～08、SDD、Sprint 与 bugfix report 同步。

## 码道独立审查与直接修正

初版存在 ECS Agency 和 OBS SDK 参数不符合当前 SDK、上传 header 类型错误、OBS 临时路径离开上下文即失效、delete 未检查状态、生产校验过宽、ReDoc 未关闭、迁移绕过 Secret 入口、数据库 DSN 暴露、前端构建上下文错误、Nginx 非 root 只读启动失败、镜像占位符非法和部署文档端口/SSL/RPO 口径不实等问题。码道按授权直接修正，没有增加返工轮次。

## 轻量验收

| 项目 | 结果 |
|---|---|
| P8.4 后端定向 | 3 passed，5 个第三方弃用警告 |
| 后端生产镜像 | 构建成功；非 root/read-only；live 200；docs 404；无 pytest/tests |
| 前端生产镜像 | 140 modules；非 root/read-only/cap_drop ALL；healthz 与首页成功 |
| Compose 静态配置 | 占位 IP fail-closed；显式假值 `config --quiet` 通过 |
| 全量/迁移/真实云 | 按轻量策略未执行；留待实际发布窗口 |
| 开发库与现有容器 | 未用于测试，未替换或修改 |

P8.4 完成后不再生成新的开发轮次。下一步是用户准备华为云资源和 Secret 后，按 `deploy/huawei/README.md` 执行真实部署与小额业务验收。

## 轻量单机真实部署补充

为控制实习项目成本，码道新增 `docker-compose.single.yml`、`.env.single.example`、`nginx.single.conf`、`Dockerfile.single` 和独立操作说明。该档案不修改 P8.4 正式部署架构，仅将当前真实部署路径调整为单台 ECS 本地构建、PostgreSQL 与论文文件本地持久化、华为 MaaS 远程调用。

当前已完成 ECS、Docker、镜像加速和 2 GiB Swap 准备；单机 Compose `config --quiet` 与 2 个定向测试通过。当前源码已在真实 ECS 构建启动，frontend、backend、postgres 状态正常，迁移成功，live 与 ready 健康检查通过；公网访问和小额业务验收仍待执行。

### Bug 列表

| 报告ID | 描述 | 严重程度 | 日期 | 链接 |
|--------|------|----------|------|------|
| 20260717_184708 | 单机部署前端 Nginx 临时目录权限错误 | major | 2026-07-17 | [查看](../bugfix-report/frontend/20260717_184708_Nginx_FIX.md) |
