# P8.3 性能、可靠性、限流与可观测性收口 — Sprint 记录

## 文档信息

- **项目名称**: PaperLens
- **Sprint 阶段**: P8.3
- **创建日期**: 2026-07-16
- **最后更新**: 2026-07-16
- **实现者**: CodeArts（码道）

## 目标

在 P8.2 已验收基础上，补齐应用级限流、请求追踪、安全结构化日志、健康/就绪检查、数据库连接池参数和恢复派发并发上限。

## 完成项

### 基础设施

- [x] 新增 P8.3 配置项到 `config.py`（rate_limit_*、db_pool_*、recovery_max_workers）
- [x] 实现 `request_tracing.py`（X-Request-ID + contextvars + 结构化日志）
- [x] 实现 `rate_limiter.py`（固定窗口限流器 + classify_scope + resolve_client_ip + parse_trusted_cidrs）
- [x] 实现 `rate_limit_middleware.py`（限流中间件 + 429 响应）
- [x] 更新 `health.py`（新增 /health/live 和 /health/ready 端点）
- [x] 更新 `database.py`（pool_pre_ping + 有界池参数 + _build_engine_kwargs）
- [x] 更新 `recovery_service.py`（ThreadPoolExecutor 替代 daemon Thread + shutdown_executor + submitter 注入）
- [x] 更新 `main.py`（中间件注册 + executor shutdown 在 lifespan 中）
- [x] 更新 `errors.py`（所有错误响应返回 X-Request-ID + 结构化日志）

### 性能修复

- [x] 修复 `list_reviews` N+1 查询（selectinload 急加载 findings + evidences）
- [x] 修复 `list_qa_conversations` N+1 查询（批量聚合查询替代逐会话查询）
- [x] 修复 `list_tasks` 无界查询（添加 .limit(200)）
- [x] 审查 `list_evidences`：撤销会静默丢失论文证据的固定 .limit(500)，保持现有完整契约

### 测试

- [x] 编写 3 个后端测试函数（test_p83_observability.py）

### 文档

- [x] 更新 02-架构设计.md（P8.3 架构章节）
- [x] 更新 04-API接口设计.md（P8.3 接口影响章节）
- [x] 更新 08-测试设计.md（P8.3 测试章节）
- [x] 新增 SDD design/18-性能可靠性与可观测性.md
- [x] 新增 Sprint 记录

## 新增/修改文件清单

| 文件 | 操作 |
|------|------|
| `backend/paperlens/core/config.py` | 修改 |
| `backend/paperlens/core/request_tracing.py` | 新增 |
| `backend/paperlens/core/rate_limiter.py` | 新增 |
| `backend/paperlens/core/rate_limit_middleware.py` | 新增 |
| `backend/paperlens/api/health.py` | 修改 |
| `backend/paperlens/core/database.py` | 修改 |
| `backend/paperlens/services/recovery_service.py` | 修改 |
| `backend/paperlens/main.py` | 修改 |
| `backend/paperlens/core/errors.py` | 修改 |
| `backend/paperlens/api/tasks.py` | 修改 |
| `backend/paperlens/services/qa_service.py` | 修改 |
| `backend/paperlens/api/papers.py` | 修改 |
| `backend/tests/test_api/test_p83_observability.py` | 新增 |

## 验收状态

| 验证项 | 结果 |
|---|---|
| P8.3 三项定向 | 3 passed，5 个第三方弃用警告 |
| 审阅/问答查询回归 | 2 passed，5 个第三方弃用警告 |
| 后端镜像构建 | 成功 |
| 隔离 HTTP 烟测 | live=200、ready=200、missing=404 |
| 全量/前端/压测 | 按轻量策略未执行 |

状态：已完成并经码道独立收口。新后端镜像已构建；为避免默认 startup recovery 扫描开发库，未替换正在运行的旧后端容器。
