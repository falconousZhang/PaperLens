# PaperLens PC端页面原型设计

## 文档信息

- **项目名称**: PaperLens
- **文档版本**: v1.1
- **创建日期**: 2026-07-13
- **最后更新**: 2026-07-13
- **关联文档**: 06-需求规格说明.md, 04-API接口设计.md

## 页面原型总览

| 编号 | 页面名称 | 路由 | 实现状态 | 功能概述 |
|------|----------|------|----------|----------|
| P01 | 首页 | `/` | 已实现 | 项目入口，后端健康检查，快捷导航 |
| P02 | 论文上传 | `/upload` | 已实现 | PDF 拖拽/选择上传，进度显示 |
| P03 | 论文列表 | `/papers` | 已实现 | 论文表格列表，状态轮询，分页 |
| P04 | 论文详情 | `/papers/:id` | 已实现 | 章节/页面/证据三 Tab，Evidence 高亮跳转 |
| P05 | 审阅结果 | `/papers/:id/review` | ✅ CURRENT | 按 task 归组展示审阅 Finding，任务交互与 Evidence 追溯 |
| P06 | 指标分析 | `/papers/:id/metrics` | ✅ CURRENT | 指标任务交互、历史结果隔离、筛选分页与来源追溯 |
| P07 | 实验数据 | `/papers/:id/experiment` | 🔴 PLANNED | P5.1 仅后端上传/结构 API；页面、统计和交叉验证仍未实现 |
| P08 | 报告导出 | `/papers/:id/export` | 🔴 PLANNED | 多格式导出配置，进度轮询，下载 |

## 1. P01 首页 (HomeView)

### 1.1 页面布局（线框图）

```
+==================================================================+
|                        PaperLens                                  |
|                  AI 驱动的学术论文审阅助手                          |
+==================================================================+
|                                                                    |
|  +--------------------------------------------------------------+  |
|  |  [●] 后端服务正常 (v0.1.0)                                    |  |
|  +--------------------------------------------------------------+  |
|                                                                    |
|           +------------------+  +------------------+               |
|           |    上传论文       |  |    论文列表       |               |
|           +------------------+  +------------------+               |
|                                                                    |
+==================================================================+
```

### 1.2 设计说明

| 元素 | 说明 |
|------|------|
| 标题区 | 居中显示项目名 PaperLens 和副标题 |
| 状态卡 | 显示后端健康检查结果：绿色正常 / 红色不可用 / 灰色检测中 |
| 操作按钮 | 两个主操作入口：上传论文（primary）、论文列表（secondary） |

### 1.3 交互行为

| 交互 | 行为 |
|------|------|
| 页面加载 | 调用 `GET /api/v1/health`，根据结果显示状态卡 |
| 点击"上传论文" | 路由跳转至 `/upload` |
| 点击"论文列表" | 路由跳转至 `/papers` |
| 后端不可用 | 状态卡显示红色圆点 + 错误信息，按钮仍可点击 |

---

## 2. P02 论文上传 (UploadView)

### 2.1 页面布局（线框图）

```
+==================================================================+
|  上传论文                                                          |
+==================================================================+
|                                                                    |
|  +--------------------------------------------------------------+  |
|  |                                                              |  |
|  |          拖拽 PDF 文件到此处，或点击选择文件                    |  |
|  |                                                              |  |
|  |                     [选择文件]                                |  |
|  |                                                              |  |
|  +--------------------------------------------------------------+  |
|                                                                    |
|  [已选文件: attention.pdf (1.2 MB)]                                |
|                                                                    |
|  +--------------------------------------------------------------+  |
|  |████████████████████████████████████████░░░░░░░░░░░░| 78%      |  |
|  +--------------------------------------------------------------+  |
|                                                                    |
|  [上传]                                                            |
|                                                                    |
|  返回列表                                                          |
+==================================================================+
```

### 2.2 设计说明

| 元素 | 说明 |
|------|------|
| 拖拽区 | 虚线边框区域，支持 drag-drop 和点击选择 |
| 文件信息 | 选择文件后显示文件名和大小 |
| 进度条 | 上传中显示进度百分比 |
| 错误提示 | 非 PDF 文件 / 超过 50MB 时显示红色错误信息 |
| 上传按钮 | 选择文件后显示，上传中隐藏 |

### 2.3 交互行为

| 交互 | 行为 |
|------|------|
| 拖拽文件进入 | 拖拽区边框变深色，背景变浅紫 |
| 选择/拖拽文件 | 校验文件类型（.pdf）和大小（<=50MB），不合规显示错误 |
| 点击"上传" | 调用 `POST /api/v1/papers/upload`，显示进度条 |
| 上传成功 | 路由跳转至 `/papers/:id`（论文详情页） |
| 上传失败 | 显示错误信息，保留文件选择状态 |
| 点击"返回列表" | 路由跳转至 `/papers` |

---

## 3. P03 论文列表 (PaperListView)

### 3.1 页面布局（线框图）

```
+==================================================================+
|  论文列表                                                          |
+==================================================================+
|                                                                    |
|  +--------------------------------------------------------------+  |
|  | 标题        | 文件名         | 状态   | 页数 | 创建时间     |操作|  |
|  |-------------|----------------|--------|------|-------------|----|  |
|  | Attention.. | attention.pdf  | 已解析 | 12   | 2026-07-12  |查看|  |
|  | BERT Paper  | bert.pdf       | 解析中 | -    | 2026-07-12  |查看|  |
|  | GPT-4 Study | gpt4.pdf       | 失败   | -    | 2026-07-11  |查看|  |
|  +--------------------------------------------------------------+  |
|                                                                    |
|  上传新论文                                                        |
+==================================================================+
```

### 3.2 设计说明

| 元素 | 说明 |
|------|------|
| 表格列 | 标题、文件名、状态、页数、创建时间、操作 |
| 状态标签 | 解析中(橙色)、已解析(绿色)、失败(红色)、上传中(灰色) |
| 空状态 | 无论文时显示"暂无论文"和上传链接 |
| 错误状态 | 加载失败时显示错误信息和重试按钮 |

### 3.3 交互行为

| 交互 | 行为 |
|------|------|
| 页面加载 | 调用 `GET /api/v1/papers` 获取列表 |
| 存在 PROCESSING 状态 | 启动 3 秒间隔轮询，直到无 PROCESSING 论文 |
| 点击"查看" | 路由跳转至 `/papers/:id` |
| 点击"上传新论文" | 路由跳转至 `/upload` |
| 加载失败 | 显示错误信息和重试按钮 |
| 组件卸载 | 清除轮询定时器 |

---

## 4. P04 论文详情 (PaperDetailView)

### 4.1 页面布局（线框图）

**整体结构**：

```
+==================================================================+
|  Attention Is All You Need                                        |
|  文件: attention.pdf | 大小: 1.2 MB | 页数: 12 | 已解析           |
+==================================================================+
|  [章节]  [页面]  [证据]                                            |
+==================================================================+
|                                                                    |
|  (当前 Tab 内容区)                                                 |
|                                                                    |
+==================================================================+
|  返回列表                                                          |
+==================================================================+
```

**章节 Tab**：

```
+------------------------------------------------------------------+
|  +--------------------------------------------------------------+  |
|  | Abstract                                                     |  |
|  | ABSTRACT | 页 1-1                                             |  |
|  | We propose a new simple network architecture, the Transformer|  |
|  | ...                                                          |  |
|  +--------------------------------------------------------------+  |
|  +--------------------------------------------------------------+  |
|  | 1 Introduction                                               |  |
|  | INTRODUCTION | 页 1-3                                         |  |
|  | Recurrent neural networks and language models...             |  |
|  | ...                                                          |  |
|  +--------------------------------------------------------------+  |
+------------------------------------------------------------------+
```

**页面 Tab**：

```
+------------------------------------------------------------------+
|  [上一页]  第 3 / 12 页  [下一页]  [___] [跳转]                   |
+------------------------------------------------------------------+
|                                                                    |
|  +--------------------------------------------------------------+  |
|  |                                                              |  |
|  |  ...Our model achieves ██████████████████ on SQuAD 2.0...    |  |
|  |                    ^^^^^^^^^^^^^^^^^^^^^^^^                  |  |
|  |                    高亮 Evidence 区域                         |  |
|  |                                                              |  |
|  +--------------------------------------------------------------+  |
|                                                                    |
+------------------------------------------------------------------+
```

**证据 Tab**：

```
+------------------------------------------------------------------+
|  +--------------------------------------------------------------+  |
|  | 页 7 | TEXT                                                  |  |
|  | Our model achieves 89.1% accuracy on SQuAD...                |  |
|  +--------------------------------------------------------------+  |
|  +--------------------------------------------------------------+  |
|  | 页 5 | TABLE                                                |  |
|  | Table 1: Main results on SQuAD 2.0...                        |  |
|  +--------------------------------------------------------------+  |
+------------------------------------------------------------------+
```

### 4.2 设计说明

| 元素 | 说明 |
|------|------|
| 元信息栏 | 文件名、大小、页数、状态标签 |
| 三 Tab 切换 | 章节 / 页面 / 证据 |
| 章节卡片 | 标题、类型、页码范围、文本预览（截断 500 字符） |
| 页面导航 | 上一页/下一页按钮、页码显示、跳转输入框 |
| 页面内容 | normalized_text_content 文本，Evidence 高亮（黄色背景） |
| 证据列表 | 每条显示页码、类型、引用文本（截断 300 字符），可点击 |
| 降级提示 | Evidence 高亮不精确时显示橙色提示条 |
| 处理中状态 | 橙色提示"论文正在解析中" |
| 失败状态 | 红色提示，含 error_message 和重新上传链接 |

### 4.3 交互行为

| 交互 | 行为 |
|------|------|
| 页面加载 | 调用 `GET /api/v1/papers/:id`，PROCESSING 状态启动 3 秒轮询 |
| 切换 Tab | 切换章节/页面/证据内容区 |
| 切换到"页面" Tab | 自动加载当前页码的页面内容 |
| 翻页 | 上一页/下一页按钮，或输入页码跳转 |
| 点击证据项 | 自动切换到"页面" Tab，跳转到对应页码，高亮 Evidence 文本 |
| Evidence 高亮 | 基于 char_start/char_end 在 normalized_text_content 中标记 |
| 高亮定位 | 高亮后自动 scrollIntoView 至可见区域 |
| 高亮降级 | char 区间无效或与 quoted_text 不匹配时，显示降级提示 |
| 轮询失败 | 显示错误信息和重试按钮 |

---

## 5. P05 审阅结果 (ReviewResultView)

### 5.1 页面布局（线框图）

```
+==================================================================+
|  Attention Is All You Need                                        |
|  文件: attention.pdf | 页数: 12 | 已解析                          |
+==================================================================+
|  [章节]  [页面]  [证据]  [审阅]  [指标]  [实验]  [导出]           |
+==================================================================+
|                                                                    |
|  +--- 审阅概览 -------------------------------------------------+  |
|  | 总评: 4/5  | 维度数: 6  | Finding 数: 18                     |  |
|  +--------------------------------------------------------------+  |
|                                                                    |
|  +--- 维度: SOUNDNESS (评分: 4/5) -----------------------------+  |
|  |                                                              |  |
|  |  +--- STRENGTH #1 ----------------------------------------+  |  |
|  |  | Clear experimental setup with proper baselines.         |  |  |
|  |  | 置信度: 0.92  [查看证据 >]                               |  |  |
|  |  +--------------------------------------------------------+  |  |
|  |                                                              |  |
|  |  +--- WEAKNESS #2 ----------------------------------------+  |  |
|  |  | Limited dataset diversity.                              |  |  |
|  |  | 置信度: 0.85  [查看证据 >]                               |  |  |
|  |  +--------------------------------------------------------+  |  |
|  |                                                              |  |
|  |  +--- SUGGESTION #3 --------------------------------------+  |  |
|  |  | Consider evaluating on additional benchmarks.           |  |  |
|  |  | 置信度: 0.78  [查看证据 >]                               |  |  |
|  |  +--------------------------------------------------------+  |  |
|  +--------------------------------------------------------------+  |
|                                                                    |
|  +--- 维度: NOVELTY (评分: 3/5) -------------------------------+  |
|  |  ...                                                         |  |
|  +--------------------------------------------------------------+  |
|                                                                    |
|  返回列表                                                          |
+==================================================================+
```

### 5.2 设计说明

| 元素 | 说明 |
|------|------|
| 审阅概览 | 总评、维度数、Finding 总数和 overall_verdict |
| 维度卡片 | 按 dimension 分组，每个维度显示评分和摘要 |
| Finding 卡片 | 类型标签（STRENGTH 绿/WEAKNESS 红/SUGGESTION 蓝）、内容、置信度 |
| 结果归组 | 优先展示 tasks 倒序中最新且已有结果的 task_id，不混合历史任务 |
| 证据链接 | 每个 evidence_id 独立跳转到 PaperDetail，并在同论文 Evidence 列表精确匹配后高亮 |
| 状态区 | 空状态、活动任务进度、失败/取消、成功无结果、加载或轮询错误均有明确恢复入口 |

### 5.3 交互行为

| 交互 | 行为 |
|------|------|
| 页面加载 | 并行加载论文、tasks 和 reviews；恢复最新 PENDING/RUNNING REVIEW 任务 |
| 点击"查看证据" | 跳转 `/papers/:id?evidence=:evidence_id`，由 PaperDetail 切到页面 Tab 并高亮 |
| 无审阅结果 | 显示"发起审阅"按钮，点击调用 `POST /api/v1/papers/:id/tasks` |
| 审阅进行中 | 显示限制在 0～100 的进度条，3 秒轮询任务状态；活动任务期间禁止重复创建 |
| 重新审阅 | 新任务成功前继续显示上一轮成功结果，并单独显示进度或失败状态 |
| 轮询失败 | 停止 timer，保留任务状态并提供“重试轮询/重新加载”；始终最多一个 timer |
| Finding 类型筛选 | 可按 STRENGTH / WEAKNESS / SUGGESTION 筛选 |

### 5.4 API 依赖

| API | 用途 |
|-----|------|
| `GET /api/v1/papers/:id/reviews` | 获取审阅结果 |
| `GET /api/v1/papers/:id/tasks` | 获取任务历史并恢复活动任务 |
| `POST /api/v1/papers/:id/tasks` | 创建审阅任务（task_type=REVIEW） |
| `GET /api/v1/tasks/:task_id` | 轮询任务进度 |

---

## 6. P06 指标分析 (MetricAnalysisView)

### 6.1 页面布局（线框图）

```
+==================================================================+
|  Attention Is All You Need                                        |
|  文件: attention.pdf | 页数: 12 | 已解析                          |
+==================================================================+
|  [章节]  [页面]  [证据]  [审阅]  [指标]  [实验]  [导出]           |
+==================================================================+
|                                                                    |
|  历史任务: [最近成功任务 ▼]                                      |
|  筛选: [指标名]  [数据集]  [Checkpoint ▼]                         |
|                                                                    |
|  +--------------------------------------------------------------+  |
|  | 模型       | 数据集    | 指标 | 显示值 | Checkpoint | 来源      |  |
|  |------------|----------|------|--------|------------|-----------|  |
|  | BERT-base  | SQuAD    | F1   | 83.1%  | BEST      | 查看证据  |  |
|  | BERT-large | SQuAD    | loss | 0.234  | FINAL     | 表格/行号 |  |
|  +--------------------------------------------------------------+  |
|                                                                    |
|  +--- 来源原文 -------------------------------------------------+  |
|  | 存储值: 0.831；raw_text 可展开查看                           |  |
|  | Evidence: [查看证据 >]；表格: table_id / 0-based row_index   |  |
|  +--------------------------------------------------------------+  |
|                                                                    |
|  返回列表                                                          |
+==================================================================+
```

### 6.2 设计说明

| 元素 | 说明 |
|------|------|
| 历史任务 | 仅列出成功的指标任务，默认选择后端顺序中的最新成功任务，查询始终携带 `task_id` |
| 筛选栏 | 指标名、数据集精确筛选和 Checkpoint 下拉，筛选由后端分页查询完成 |
| 指标表格 | 模型、数据集、指标名、显示值/存储值、Checkpoint、来源、原文和创建时间 |
| 口径标签 | FINAL/MAX/MEAN/BEST/LAST/UNKNOWN 均有中文文本与稳定样式 |
| 来源追溯 | 单一 Evidence 生成论文详情深链；单一表格来源显示 `table_id` 与 0-based `row_index`；双来源或无来源安全降级 |
| 原文 | `raw_text` 仅以文本插值输出，可展开且长文本换行，不使用 `v-html` |
| 空状态 | 未提取指标时显示"提取指标"按钮 |

### 6.3 交互行为

| 交互 | 行为 |
|------|------|
| 页面加载 | 并行读取论文与任务；只处理 `METRIC_EXTRACTION`，选择最新成功任务后携带 `task_id` 查询指标 |
| 筛选/分页/任务变更 | 重置必要分页并重新请求；独立请求序号阻止旧响应覆盖新目标 |
| 点击"查看证据" | 跳转 `{name:'paper-detail', query:{evidence:evidence_id}}` 并由论文详情高亮 |
| 无指标数据 | 显示"提取指标"按钮，调用 `POST /api/v1/papers/:id/tasks`（task_type=METRIC_EXTRACTION） |
| 提取进行中 | 恢复或创建活动任务后每 3 秒轮询，保留上一轮成功结果并锁定重复创建 |
| 任务终态 | SUCCEEDED 刷新任务并切换到新 `task_id`；FAILED/CANCELLED 保留旧结果并允许显式重试 |
| 生命周期 | 论文路由变化、连续请求和组件卸载均使旧异步结果失效并清理 timer |

### 6.4 API 依赖

| API | 用途 |
|-----|------|
| `GET /api/v1/papers/:id/metrics` | 获取指标记录 |
| `GET /api/v1/papers/:id/tasks` | 获取任务历史并恢复活动指标任务 |
| `GET /api/v1/tasks/:task_id` | 轮询指标任务状态 |
| `POST /api/v1/papers/:id/tasks` | 创建指标提取任务 |

---

## 7. P07 实验数据 (ExperimentDataView)

### 7.1 页面布局（线框图）

```
+==================================================================+
|  Attention Is All You Need                                        |
|  文件: attention.pdf | 页数: 12 | 已解析                          |
+==================================================================+
|  [章节]  [页面]  [证据]  [审阅]  [指标]  [实验]  [导出]           |
+==================================================================+
|                                                                    |
|  +--- 上传实验数据 ---------------------------------------------+  |
|  |  [拖拽 CSV/Excel 文件到此处，或点击选择]  [选择文件]          |  |
|  +--------------------------------------------------------------+  |
|                                                                    |
|  +--- 已上传文件 -----------------------------------------------+  |
|  | 文件名              | 类型 | 行数 | 列数 | 上传时间  | 操作 |  |
|  |---------------------|------|------|------|----------|------|  |
|  | experiment_results  | CSV  | 50   | 8    | 07-12    | 删除 |  |
|  +--------------------------------------------------------------+  |
|                                                                    |
|  +--- 统计摘要 -------------------------------------------------+  |
|  | 列名       | count | mean   | std    | min    | max    |median|  |
|  |------------|-------|--------|--------|--------|--------|------|  |
|  | accuracy   | 10    | 0.852  | 0.023  | 0.810  | 0.891  |0.855 |  |
|  | f1_score   | 10    | 0.831  | 0.018  | 0.790  | 0.861  |0.835 |  |
|  +--------------------------------------------------------------+  |
|                                                                    |
|  +--- 交叉验证结果 ---------------------------------------------+  |
|  | 指标名    | 数据集  | 论文值  | 实验值  | 偏差   | 状态     |  |
|  |-----------|---------|---------|---------|--------|----------|  |
|  | accuracy  | SQuAD   | 0.891   | 0.855   | -0.036 | MISMATCH |  |
|  | f1_score  | SQuAD   | 0.861   | 0.861   | 0.000  | MATCH    |  |
|  +--------------------------------------------------------------+  |
|                                                                    |
|  返回列表                                                          |
+==================================================================+
```

### 7.2 设计说明

| 元素 | 说明 |
|------|------|
| 上传区 | 拖拽/选择上传 CSV/XLSX/XLS，最大 20MB |
| 文件列表 | 已上传文件表格，含文件名、类型、行列数、删除操作 |
| 统计摘要 | 每列的 count/mean/std/min/max/median，由确定性 Python 代码计算 |
| 交叉验证 | 论文指标 vs 实验数据对比，MATCH(绿)/MISMATCH(红) 状态标签 |
| 偏差值 | diff 列，正/负以不同颜色标注 |

### 7.3 交互行为

| 交互 | 行为 |
|------|------|
| 上传文件 | 校验类型和大小，调用 `POST /api/v1/papers/:id/experiment-files/upload` |
| 上传成功 | 刷新文件列表和统计摘要 |
| 点击"删除" | 确认弹窗，调用 `DELETE /api/v1/experiment-files/:file_id` |
| 页面加载 | 调用 `GET /api/v1/papers/:id/experiment-files` 获取文件列表 |
| 选择文件 | 调用 `GET /api/v1/experiment-files/:file_id/result` 获取分析结果 |
| 无文件 | 仅显示上传区，不显示统计和验证区 |

### 7.4 API 依赖

| API | 用途 |
|-----|------|
| `POST /api/v1/papers/:id/experiment-files/upload` | 上传实验数据 |
| `GET /api/v1/papers/:id/experiment-files` | 获取文件列表 |
| `GET /api/v1/experiment-files/:file_id/result` | 获取分析结果 |
| `DELETE /api/v1/experiment-files/:file_id` | 删除文件 |

---

## 8. P08 报告导出 (ReportExportView)

### 8.1 页面布局（线框图）

```
+==================================================================+
|  Attention Is All You Need                                        |
|  文件: attention.pdf | 页数: 12 | 已解析                          |
+==================================================================+
|  [章节]  [页面]  [证据]  [审阅]  [指标]  [实验]  [导出]           |
+==================================================================+
|                                                                    |
|  +--- 导出配置 -------------------------------------------------+  |
|  |                                                              |  |
|  |  报告格式:  (●) Markdown  ( ) PDF  ( ) DOCX                  |  |
|  |                                                              |  |
|  |  报告语言:  [中文 ▼]                                         |  |
|  |                                                              |  |
|  |  [✓] 包含指标记录                                             |  |
|  |  [✓] 包含实验分析                                             |  |
|  |                                                              |  |
|  |  [生成报告]                                                   |  |
|  +--------------------------------------------------------------+  |
|                                                                    |
|  +--- 导出历史 -------------------------------------------------+  |
|  | 格式      | 状态   | 文件大小  | 创建时间     | 操作        |  |
|  |-----------|--------|----------|-------------|-------------|  |
|  | Markdown  | 已就绪 | 20 KB    | 2026-07-12  | [下载]      |  |
|  | PDF       | 生成中 | -        | 2026-07-12  | (进度 65%)  |  |
|  +--------------------------------------------------------------+  |
|                                                                    |
|  返回列表                                                          |
+==================================================================+
```

### 8.2 设计说明

| 元素 | 说明 |
|------|------|
| 格式选择 | Markdown / PDF / DOCX 单选 |
| 语言选择 | 中文 / 英文 下拉 |
| 包含选项 | 包含指标记录、包含实验分析 复选框 |
| 生成按钮 | 提交导出请求 |
| 导出历史 | 历史导出列表，含状态、文件大小、下载操作 |
| 进度显示 | 生成中显示进度百分比，3 秒轮询 |
| 下载按钮 | 状态为 READY 时显示 |

### 8.3 交互行为

| 交互 | 行为 |
|------|------|
| 点击"生成报告" | 调用 `POST /api/v1/papers/:id/exports`，添加到历史列表 |
| 生成中 | 3 秒轮询 `GET /api/v1/exports/:export_id`，显示进度 |
| 生成完成 | 状态变为 READY，显示文件大小和下载按钮 |
| 点击"下载" | 调用 `GET /api/v1/exports/:export_id/download`，触发浏览器下载 |
| 生成失败 | 显示错误信息 |

### 8.4 API 依赖

| API | 用途 |
|-----|------|
| `POST /api/v1/papers/:id/exports` | 生成导出报告 |
| `GET /api/v1/exports/:export_id` | 获取导出状态 |
| `GET /api/v1/exports/:export_id/download` | 下载报告文件 |

---

## 9. 页面结构说明

### 9.1 导航架构

当前实现采用平铺路由，无全局导航栏。P05/P06 已通过论文详情“审阅”“指标”入口接入；P07-P08 上线后可再评估统一子页面导航：

```
首页 (/)
  ├── 上传论文 (/upload)
  └── 论文列表 (/papers)
        └── 论文详情 (/papers/:id)
              ├── 章节 Tab
              ├── 页面 Tab
              ├── 证据 Tab
              ├── 审阅 (/papers/:id/review)
              ├── 指标 (/papers/:id/metrics)
              ├── 实验 (/papers/:id/experiment)
              └── 导出 (/papers/:id/export)
```

### 9.2 共享布局

P05/P06 当前复用相同的论文标题与元信息视觉，但仍是独立平铺路由。P07-P08 实现时可评估提取 `PaperDetailLayout`：

```
+==================================================================+
|  {paper.title}                                                    |
|  文件: {filename} | 大小: {size} | 页数: {pages} | {status}       |
+==================================================================+
|  [章节] [页面] [证据] [审阅] [指标] [实验] [导出]                  |
+==================================================================+
|                                                                    |
|  <router-view />  (子页面内容)                                     |
|                                                                    |
+==================================================================+
```

### 9.3 路由规划

```typescript
{ path: '/papers/:id', component: PaperDetailLayout, children: [
  { path: '',          name: 'paper-detail',  component: PaperDetailContent },
  { path: 'review',    name: 'paper-review',  component: ReviewResultView },
  { path: 'metrics',   name: 'paper-metrics', component: MetricAnalysisView },
  { path: 'experiment',name: 'paper-experiment', component: ExperimentDataView },
  { path: 'export',    name: 'paper-export',  component: ReportExportView },
]}
```

---

## 10. 页面交互流程

### 10.1 核心业务流程

```
用户访问首页
    │
    ├──> 上传论文 ──> 上传成功 ──> 论文详情(解析中)
    │                                    │
    │                              轮询直到解析完成
    │                                    │
    │                              论文详情(已解析)
    │                               │  │  │  │
    │                    ┌──────────┘  │  │  └──────────┐
    │                    ↓             ↓  ↓             ↓
    │                 查看章节     查看页面  查看证据   发起审阅
    │                                            │
    │                                      审阅进行中(轮询)
    │                                            │
    │                                      审阅完成
    │                                       │
    │                              ┌────────┼────────┐
    │                              ↓        ↓        ↓
    │                          查看Finding 查看指标  导出报告
    │                              │
    │                              ↓
    │                        点击Evidence
    │                              │
    │                              ↓
    │                        跳转页面Tab高亮
    │
    └──> 论文列表 ──> 选择论文 ──> 论文详情
```

### 10.2 Evidence 追溯流程

```
审阅结果页/指标分析页
    │
    ├── Finding 卡片中的"查看证据"链接
    │
    ↓
切换到页面 Tab
    │
    ↓
跳转到 Evidence 所在页码
    │
    ↓
基于 char_start / char_end 高亮文本
    │
    ├── 高亮成功 → scrollIntoView 定位
    │
    └── 高亮失败 → 显示降级提示
```

### 10.3 实验数据交叉验证流程

```
论文详情页 → 实验数据 Tab
    │
    ├── 上传 CSV/Excel 文件
    │       │
    │       ↓
    │   后端解析 + 统计计算（确定性代码）
    │       │
    │       ↓
    │   展示统计摘要
    │       │
    │       ↓
    │   自动与论文 MetricRecord 交叉验证
    │       │
    │       ↓
    │   展示 MATCH / MISMATCH 结果
    │
    └── 查看已有文件的分析结果
```

---

## 11. 设计规范总结

### 11.1 布局规范

| 项目 | 规范 |
|------|------|
| 页面最大宽度 | 960px（居中） |
| 页面内边距 | 2rem 上下，1rem 左右 |
| 字体 | -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif |
| 主色调 | #1a1a2e（深蓝黑） |
| 背景色 | #fff（白） |
| 卡片圆角 | 8px |
| 按钮圆角 | 6-8px |

### 11.2 组件规范

| 组件 | 规范 |
|------|------|
| 主按钮 | 背景 #1a1a2e，白色文字，padding: 0.75rem 2rem，圆角 8px |
| 次按钮 | 背景 #fafafa，边框 #e0e0e0，padding: 0.5rem 1.5rem，圆角 6px |
| Tab 按钮 | 默认 #fafafa 边框 #e0e0e0，激活 #1a1a2e 白色 |
| 表格 | 全宽，border-collapse，行高 0.75rem padding，底部分割线 #e0e0e0 |
| 卡片 | padding 1rem，边框 #e0e0e0，圆角 8px |
| 输入框 | padding 0.3rem，边框 #e0e0e0，圆角 4px |
| 拖拽区 | 虚线边框 2px dashed #ccc，padding 3rem，居中文字 |

### 11.3 状态色规范

| 状态 | 文字色 | 背景色 | 用途 |
|------|--------|--------|------|
| 正常/成功 | #2e7d32 | #e8f5e9 | 已解析、MATCH、健康 |
| 警告/进行中 | #f57c00 / #e65100 | #fff3e0 | 解析中、PROCESSING |
| 错误/失败 | #c62828 | #ffebee | 失败、MISMATCH、错误 |
| 高亮 | - | #fff176 | Evidence 高亮 |
| 降级 | #e65100 | #fff3e0 | 高亮降级提示 |

### 11.4 交互规范

| 场景 | 规范 |
|------|------|
| 加载状态 | 显示"加载中..."文字提示 |
| 轮询间隔 | 3 秒（PROCESSING 状态、任务进度、导出进度） |
| 错误处理 | 显示错误信息 + 重试按钮 |
| 文件校验 | 前端预校验类型和大小，后端再次校验 |
| 确认操作 | 删除操作需二次确认弹窗 |
| 进度展示 | 上传进度条、任务进度百分比、导出进度 |
| 路由跳转 | 上传成功后自动跳转详情页 |
| 组件卸载 | 清除所有轮询定时器 |
| 认证启动 | Router guard 等待一次 refresh bootstrap，失败进入匿名状态 |
| 安全跳转 | 仅允许单斜杠开头的站内 redirect，拒绝绝对 URL、`//` 与反斜杠路径 |

### 11.6 P3.5 认证页面（CURRENT）

| 页面 | 路由 | 关键状态 |
|------|------|----------|
| 登录 | `/login` | 统一安全错误、提交锁、成功回安全站内 redirect |
| 注册 | `/register` | 邮箱、15～128 字符密码、确认密码、显示名校验 |
| 找回密码 | `/forgot-password` | 始终显示“若账号存在且通知服务可用”统一提示 |
| 重置密码 | `/reset-password?token=...` | 非单一字符串 token 阻止提交，成功后回登录 |
| 个人资料 | `/profile` | email/role/status、显示名、改密、logout-all |

access token 与 UserPublic 只保存在 Pinia 内存；refresh cookie 不可由 JavaScript 读取。上传、论文、详情、审阅和个人资料页均为 requiresAuth。

### 11.5 数据展示规范

| 数据类型 | 格式 |
|----------|------|
| 文件大小 | MB，保留 1 位小数（如 1.2 MB） |
| 日期时间 | toLocaleString('zh-CN') |
| 状态标签 | 中文映射：UPLOADING→上传中, PROCESSING→解析中, PARSED→已解析, FAILED→失败 |
| 文本截断 | 章节预览 500 字符，证据预览 300 字符 |
| 百分比 | 整数（如 78%） |
| 置信度 | 2 位小数（如 0.92） |
| 指标值 | 保留原始精度 |

---

## P4.3 页面影响

本阶段没有新增或修改 UI。MaaS API Key、模型和 endpoint 不进入浏览器表单、Web Storage 或前端构建变量；运行配置只由 backend 环境和 CLI 管理。

**文档版本**：v1.2
**创建日期**：2026-07-13
**最后更新**：2026-07-14
